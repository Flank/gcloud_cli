options:
  inputMappings:
  - fieldName: Authorization
    value: $.BasicAuth($.type.my_secrets, $.type.secrets)
    location: HEADER
credential:
  basicAuth:
    user: cool_user
    password: cooler_password
