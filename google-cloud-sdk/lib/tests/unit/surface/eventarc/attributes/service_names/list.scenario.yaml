title: eventarc attributes service-names list test scenario
release_tracks: [BETA]

summary:
# This summary is generated by http://go/gcloud-scenario-tests on update and should not be edited.
- execute:
  - command: |
      eventarc attributes service-names list
        --type google.cloud.audit.log.v1.written
  - stdout: |-
      SERVICE_NAME +DISPLAY_NAME
      .*bigquery.googleapis.com +BigQuery
      .*storage.googleapis.com +Cloud Storage.*
      $
- execute:
  - command: |
      eventarc attributes service-names list
        --type google.cloud.pubsub.topic.v1.messagePublished
  - error: '1: For this command, the event type must be: google.cloud.audit.log.v1.written.'

actions:
- execute_command:
    command: |
      eventarc attributes service-names list
        --type google.cloud.audit.log.v1.written
    events:
    - expect_stdout:
        matches: |
          SERVICE_NAME +DISPLAY_NAME
          .*bigquery.googleapis.com +BigQuery
          .*storage.googleapis.com +Cloud Storage.*
    - expect_exit:
        code: 0

- execute_command:
    command: |
      eventarc attributes service-names list
        --type google.cloud.pubsub.topic.v1.messagePublished
    events:
    - expect_exit:
        code: 1
        message: 'For this command, the event type must be: google.cloud.audit.log.v1.written.'
