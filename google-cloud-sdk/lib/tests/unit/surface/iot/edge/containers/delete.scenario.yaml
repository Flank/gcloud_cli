title: cloud iot edge containers delete test scenario
release_tracks: [ALPHA]
summary:
# This summary is generated automatically on update and should not be edited.
- execute:
  - command: |
      iot edge containers delete foo --region asia-east1 --registry my-registry --device my-device
  - prompt:
    - message: You are about to delete container [foo]
    - input: y
  - stderr: |
      Deleted container [foo].
actions:
- execute_command:
    command: |
      iot edge containers delete foo --region asia-east1 --registry my-registry --device my-device
    events:
    - expect_prompt_continue:
        message: You are about to delete container [foo]
        user_input: y
    - api_call:
        expect_request:
          uri: https://edge.googleapis.com/v1alpha1/projects/fake-project/locations/asia-east1/registries/my-registry/devices/my-device/containers/foo?alt=json
          method: DELETE
          headers: {}
          body: null
        return_response:
          headers:
            status: '200'
          body: {}
    - expect_stderr: |
        Deleted container [foo].
    - expect_exit:
        code: 0
