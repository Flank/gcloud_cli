title: cloud iot edge functions delete test scenario
release_tracks: [ALPHA]
summary:
# This summary is generated automatically on update and should not be edited.
- execute:
  - command: |
      iot edge functions delete foo --region asia-east1 --registry my-registry --device my-device
  - prompt:
    - message: You are about to delete function [foo]
    - input: y
  - stderr: |
      Deleted function [foo].
actions:
- execute_command:
    command: |
      iot edge functions delete foo --region asia-east1 --registry my-registry --device my-device
    events:
    - expect_prompt_continue:
        message: You are about to delete function [foo]
        user_input: y
    - api_call:
        expect_request:
          uri: https://edge.googleapis.com/v1alpha1/projects/fake-project/locations/asia-east1/registries/my-registry/devices/my-device/functions/foo?alt=json
          method: DELETE
          headers: {}
          body: null
        return_response:
          headers:
            status: '200'
          body: {}
    - expect_stderr: |
        Deleted function [foo].
    - expect_exit:
        code: 0
