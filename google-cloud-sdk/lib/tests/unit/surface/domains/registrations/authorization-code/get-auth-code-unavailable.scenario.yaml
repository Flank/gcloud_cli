title: cloud domains authorization-code get negative test scenario
release_tracks: [ALPHA]
summary:
# This summary is generated automatically on update and should not be edited.
- execute:
  - command: |
      domains registrations authorization-code get no-authcode.com
  - error: |
      1: HTTPError 400: {
         "message": "authorization code is not available because it has been less than 60 days since you purchased the domain"
      }
actions:
- execute_command:
    command: |
      domains registrations authorization-code get no-authcode.com
    events:
    - api_call:
        expect_request:
          uri: https://domains.googleapis.com/v1alpha1/projects/fake-project/locations/global/registrations/no-authcode.com/authorizationCode?alt=json
          method: GET
          headers: {}
          body: null
        return_response:
          headers:
            status: '400'
          body: |
            {
               "message": "authorization code is not available because it has been less than 60 days since you purchased the domain"
            }
    - expect_exit:
        code: 1
        message: |
          HTTPError 400: {
             "message": "authorization code is not available because it has been less than 60 days since you purchased the domain"
          }
