title: admin-service-cluster create test with no bundles scenario
release_tracks: [ALPHA]
summary:
# This summary is generated by http://go/gcloud-scenario-tests on update and should not be edited.
- execute:
  - command: |
      admin-service-cluster instances create foo --bundles= --location=us-central1
  - error: '1: Invalid value for [--bundles]: Exactly one bundle must be enabled'
actions:
- execute_command:
    command: |
      admin-service-cluster instances create foo --bundles= --location=us-central1
    events:
    - expect_exit:
        code: 1
        message: 'Invalid value for [--bundles]: Exactly one bundle must be enabled'
