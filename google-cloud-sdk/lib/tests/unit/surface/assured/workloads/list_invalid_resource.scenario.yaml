title: assured workloads list invalid resource test
release_tracks: [BETA, ALPHA]
summary:
# This summary is generated by http://go/gcloud-scenario-tests on update and should not be edited.
- execute:
  - command: assured workloads list INVALID_RESOURCE_FORMAT --page-size=2 --limit=1
  - error: '1: Resource INVALID_RESOURCE_FORMAT was not recognised.'
actions:
- execute_command:
    command: assured workloads list INVALID_RESOURCE_FORMAT --page-size=2 --limit=1
    events:
    - expect_exit:
        code: 1
        message: Resource INVALID_RESOURCE_FORMAT was not recognised.
