NAME
    gcloud dataflow jobs cancel - cancels all jobs that match the command line
        arguments

SYNOPSIS
    gcloud dataflow jobs cancel JOB_ID [JOB_ID ...] [--region=REGION_ID]
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    Cancels all jobs that match the command line arguments.

POSITIONAL ARGUMENTS
     JOB_ID [JOB_ID ...]
        The job IDs to operate on.

FLAGS
     --region=REGION_ID
        The region ID of the jobs' regional endpoint.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

NOTES
    This variant is also available:

        $ gcloud beta dataflow jobs cancel

