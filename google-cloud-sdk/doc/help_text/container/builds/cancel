NAME
    gcloud container builds cancel - cancel an ongoing build

SYNOPSIS
    gcloud container builds cancel BUILDS [BUILDS ...] [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (DEPRECATED) Cancel an ongoing build.

    This command is deprecated and will be removed on or after 2018-10-31.
    Please use gcloud builds cancel instead.

POSITIONAL ARGUMENTS
     BUILDS [BUILDS ...]
        IDs of builds to cancel

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

NOTES
    These variants are also available:

        $ gcloud alpha container builds cancel
        $ gcloud beta container builds cancel

