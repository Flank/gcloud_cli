NAME
    gcloud ml-engine versions delete - delete an existing Cloud ML Engine
        version

SYNOPSIS
    gcloud ml-engine versions delete VERSION --model=MODEL
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    Delete an existing Cloud ML Engine version.

POSITIONAL ARGUMENTS
     VERSION
        Name of the model version.

REQUIRED FLAGS
     --model=MODEL
        Name of the model.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

NOTES
    These variants are also available:

        $ gcloud alpha ml-engine versions delete
        $ gcloud beta ml-engine versions delete

