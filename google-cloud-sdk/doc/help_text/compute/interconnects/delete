NAME
    gcloud compute interconnects delete - delete Google Compute Engine
        interconnects

SYNOPSIS
    gcloud compute interconnects delete NAME [NAME ...] [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    gcloud compute interconnects delete deletes Google Compute Engine
    interconnects. Interconnects        can only be deleted when no other resources (e.g.,
        InterconnectAttachments) refer to them.

POSITIONAL ARGUMENTS
     NAME [NAME ...]
        The names of the interconnects to delete.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

NOTES
    These variants are also available:

        $ gcloud alpha compute interconnects delete
        $ gcloud beta compute interconnects delete

