NAME
    gcloud compute accelerator-types describe - describe Google Compute Engine
        accelerator types

SYNOPSIS
    gcloud compute accelerator-types describe NAME [--zone=ZONE]
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    gcloud compute accelerator-types describe displays all data associated with
    a Google Compute Engine accelerator type.

POSITIONAL ARGUMENTS
     NAME
        Name of the accelerator type to describe.

FLAGS
     --zone=ZONE
        The zone of the accelerator type to describe. Overrides the default
        compute/zone property value for this command invocation.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

NOTES
    These variants are also available:

        $ gcloud alpha compute accelerator-types describe
        $ gcloud beta compute accelerator-types describe

