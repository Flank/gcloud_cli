NAME
    gcloud alpha category-manager assets - manage Category Manager assets

SYNOPSIS
    gcloud alpha category-manager assets COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Category Manager assets. These commands let you apply
    annotations to data assets, list data assets based on these annotations,
    and see what annotations a data asset has.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     apply-annotation
        (ALPHA) Apply an annotation to a given asset.

     delete-annotation
        (ALPHA) Deletes an annotation tag on a given asset.

     list-annotations
        (ALPHA) List all annotations for a given asset.

     search
        (ALPHA) Search for annotatable assets.

NOTES
    This command is an internal implementation detail and may change or
    disappear without notice. This command is currently in ALPHA and may change
    without notice. If this command fails with API permission errors despite
    specifying the right project, you will have to apply for early access and
    have your projects registered on the API whitelist to use it. To do so,
    contact Support at https://cloud.google.com/support/.

