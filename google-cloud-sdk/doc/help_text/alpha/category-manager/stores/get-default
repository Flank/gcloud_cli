NAME
    gcloud alpha category-manager stores get-default - get default taxonomy
        store

SYNOPSIS
    gcloud alpha category-manager stores get-default
        --organization=ORGANIZATION [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Get default taxonomy store

REQUIRED FLAGS
     Organization resource - The taxonomy stores' organization. This represents
     a Cloud resource. This must be specified.

       --organization=ORGANIZATION
          The ID of the organization or a fully qualified identifier for the
          organization.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

API REFERENCE
    This command uses the categorymanager/v1alpha2 API. The full documentation
    for this API can be found at: https://cloud.google.com/

NOTES
    This command is an internal implementation detail and may change or
    disappear without notice. This command is currently in ALPHA and may change
    without notice. If this command fails with API permission errors despite
    specifying the right project, you will have to apply for early access and
    have your projects registered on the API whitelist to use it. To do so,
    contact Support at https://cloud.google.com/support/.

