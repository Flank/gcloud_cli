NAME
    gcloud alpha category-manager taxonomies annotations - manage Category
        Manager annotations

SYNOPSIS
    gcloud alpha category-manager taxonomies annotations COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Category Manager annotations. Annotations categorize data
    assets in a defined taxonomy. Annotations can be defined in a hierarchy to
    reflect that a category can have multiple sub-categories.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create an annotation in the specified taxonomy.

     delete
        (ALPHA) Delete an annotation and all of its child annotations.

     describe
        (ALPHA) Get the details of an annotation.

     list
        (ALPHA) List all the annotations of a taxonomy.

     update
        (ALPHA) Update the description of an annotation.

NOTES
    This command is an internal implementation detail and may change or
    disappear without notice. This command is currently in ALPHA and may change
    without notice. Usually, users of ALPHA commands and flags need to apply
    for access, agree to applicable terms, and have their projects whitelisted.
    Contact Google or sign up on a product's page for ALPHA access. Product
    pages can be found at https://cloud.google.com/products/.

