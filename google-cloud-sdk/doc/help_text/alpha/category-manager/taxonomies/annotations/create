NAME
    gcloud alpha category-manager taxonomies annotations create - create an
        annotation in the specified taxonomy

SYNOPSIS
    gcloud alpha category-manager taxonomies annotations create
        --display-name=DISPLAY_NAME --taxonomy=TAXONOMY
        [--description=DESCRIPTION] [--parent-annotation=PARENT_ANNOTATION]
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Create an annotation in the specified taxonomy. By default the
    annotation is created as a root annotation, but an annotation can also be
    created as a child of another annotation by specifying the
    --parent_annotation flag.

REQUIRED FLAGS
     --display-name=DISPLAY_NAME
        A human-readable name for the annotation.

     Project taxonomy resource - The taxonomy ID of the parent taxonomy for
     this new annotation. This represents a Cloud resource. (NOTE) Some
     attributes are not given arguments in this group but can be set in other
     ways. To set the [project] attribute: Set the property [core/project] or
     provide the flag [--project] on the command line. This must be specified.

       --taxonomy=TAXONOMY
          The ID of the project_taxonomy or a fully qualified identifier for
          the project_taxonomy.

OPTIONAL FLAGS
     --description=DESCRIPTION
        The description of this annotation.

     --parent-annotation=PARENT_ANNOTATION
        The ID of the parent annotation for this annotation. If not given, this
        annotation will be at the root of the hierarchy.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

API REFERENCE
    This command uses the categorymanager/v1alpha2 API. The full documentation
    for this API can be found at: https://cloud.google.com/

NOTES
    This command is an internal implementation detail and may change or
    disappear without notice. This command is currently in ALPHA and may change
    without notice. Usually, users of ALPHA commands and flags need to apply
    for access, agree to applicable terms, and have their projects whitelisted.
    Contact Google or sign up on a product's page for ALPHA access. Product
    pages can be found at https://cloud.google.com/products/.

