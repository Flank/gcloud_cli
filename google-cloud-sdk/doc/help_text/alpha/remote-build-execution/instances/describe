NAME
    gcloud alpha remote-build-execution instances describe - describe an
        instance

SYNOPSIS
    gcloud alpha remote-build-execution instances describe INSTANCE
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Describes the given instance.

POSITIONAL ARGUMENTS
     Instance resource - The instance to describe. This represents a Cloud
     resource. (NOTE) Some attributes are not given arguments in this group but
     can be set in other ways. To set the [project] attribute: provide the
     argument [instance] on the command line with a fully specified name;
     provide the argument [--project] on the command line; set the property
     [core/project]. This must be specified.

       INSTANCE
          ID of the instance or fully qualified identifier for the instance.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity. Run $ gcloud help for
    details.

API REFERENCE
    This command uses the remotebuildexecution/v1alpha API. The full
    documentation for this API can be found at:
    https://cloud.google.com/remote-build-execution/docs/

EXAMPLES
    The following will describe the instance named 'instance1' for the active
    project:

        $ gcloud alpha remote-build-execution instances describe instance1

    You can also provide the fully qualified resource name for the instance
    like so:

        $ gcloud alpha remote-build-execution instances describe \
            projects/my_project/instances/instance1

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you will have to apply for early access and have your projects
    registered on the API whitelist to use it. To do so, contact Support at
    https://cloud.google.com/support/.

