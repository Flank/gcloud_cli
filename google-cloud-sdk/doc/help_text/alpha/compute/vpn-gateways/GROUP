NAME
    gcloud alpha compute vpn-gateways - read and manipulate Google Compute
        Engine Highly Available VPN Gateways

SYNOPSIS
    gcloud alpha compute vpn-gateways COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) read and manipulate Highly Available VPN Gateways.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity. Run $ gcloud help for
    details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create a new Google Compute Engine High Available VPN gateway.

     delete
        (ALPHA) Delete Google Compute Engine Highly Available VPN Gateways.

     describe
        (ALPHA) Describe a Google Compute Engine Highly Available VPN Gateway.

     get-status
        (ALPHA) Get status of a Google Compute Engine VPN Gateway.

     list
        (ALPHA) List Google Compute Engine Highly Available VPN Gateways.

     update
        (ALPHA) Update a Google Compute Engine Highly Available VPN gateway.

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you will have to apply for early access and have your projects
    registered on the API whitelist to use it. To do so, contact Support at
    https://cloud.google.com/support/. This variant is also available:

        $ gcloud beta compute vpn-gateways

