NAME
    gcloud alpha compute resource-policies - manage Google Compute Engine
        Resource Policies

SYNOPSIS
    gcloud alpha compute resource-policies COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Google Compute Engine Resource Policies.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity. Run $ gcloud help for
    details.

COMMANDS
    COMMAND is one of the following:

     create-snapshot-schedule
        (ALPHA) Create a Google Compute Engine Snapshot Schedule Resource
        Policy.

     create-vm-maintenance
        (ALPHA) Create a Google Compute Engine VM Maintenance Resource Policy.

     delete
        (ALPHA) Delete a Google Compute Engine resource policy.

     describe
        (ALPHA) Describe a Google Compute Engine resource policy.

     list
        (ALPHA) List Google Compute Engine resource policies.

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you will have to apply for early access and have your projects
    registered on the API whitelist to use it. To do so, contact Support at
    https://cloud.google.com/support/. This variant is also available:

        $ gcloud beta compute resource-policies

