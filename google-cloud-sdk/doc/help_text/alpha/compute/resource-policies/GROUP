NAME
    gcloud alpha compute resource-policies - manage Google Compute Engine
        Resource Policies

SYNOPSIS
    gcloud alpha compute resource-policies GROUP | COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Google Compute Engine Resource Policies.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity. Run $ gcloud help for
    details.

GROUPS
    GROUP is one of the following:

     create
        (ALPHA) Create Google Compute Engine Resource Policies.

COMMANDS
    COMMAND is one of the following:

     create-snapshot-schedule
        (ALPHA) (DEPRECATED) Create a Google Compute Engine Snapshot Schedule
        Resource Policy.

     create-vm-maintenance
        (ALPHA) (DEPRECATED) Create a Google Compute Engine VM Maintenance
        Resource Policy.

     delete
        (ALPHA) Delete a Google Compute Engine resource policy.

     describe
        (ALPHA) Describe a Google Compute Engine resource policy.

     list
        (ALPHA) List Google Compute Engine resource policies.

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you may be trying to access an API with an invitation-only early
    access whitelist. These variants are also available:

        $ gcloud compute resource-policies
        $ gcloud beta compute resource-policies

