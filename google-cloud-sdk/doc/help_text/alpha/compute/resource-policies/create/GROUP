NAME
    gcloud alpha compute resource-policies create - create Google Compute
        Engine Resource Policies

SYNOPSIS
    gcloud alpha compute resource-policies create GROUP | COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Create Google Compute Engine Resource Policies.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity.

    Run $ gcloud help for details.

GROUPS
    GROUP is one of the following:

     vm-maintenance
        (ALPHA) Create Google Compute Engine VM Maintenance Resource Policies.

COMMANDS
    COMMAND is one of the following:

     group-placement
        (ALPHA) Create a Google Compute Engine group placement resource policy.

     snapshot-schedule
        (ALPHA) Create a Google Compute Engine Snapshot Schedule Resource
        Policy.

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you may be trying to access an API with an invitation-only early
    access whitelist. These variants are also available:

        $ gcloud compute resource-policies create
        $ gcloud beta compute resource-policies create

