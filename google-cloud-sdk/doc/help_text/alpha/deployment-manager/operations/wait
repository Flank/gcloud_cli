NAME
    gcloud alpha deployment-manager operations wait - wait for all operations
        specified to complete before returning

SYNOPSIS
    gcloud alpha deployment-manager operations wait OPERATION_NAME
        [OPERATION_NAME ...] [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Polls until all operations have finished, then prints the resulting
    operations along with any operation errors.

POSITIONAL ARGUMENTS
     OPERATION_NAME [OPERATION_NAME ...]
        Operation name.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

EXAMPLES
    To poll until an operation has completed, run:

        $ gcloud alpha deployment-manager operations wait operation-name

    To poll until several operations have all completed, run:

        $ gcloud alpha deployment-manager operations wait operation-one \
            operation-two operation-three

NOTES
    This command is currently in ALPHA and may change without notice. Usually,
    users of ALPHA commands and flags need to apply for access, agree to
    applicable terms, and have their projects whitelisted. Contact Google or
    sign up on a product's page for ALPHA access. Product pages can be found at
    https://cloud.google.com/products/. These variants are also available:

        $ gcloud deployment-manager operations wait
        $ gcloud beta deployment-manager operations wait

