NAME
    gcloud alpha deployment-manager resources - commands for Deployment Manager
        resources

SYNOPSIS
    gcloud alpha deployment-manager resources COMMAND [--deployment=DEPLOYMENT]
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Commands to list and examine resources within a deployment.

FLAGS
     --deployment=DEPLOYMENT
        Deployment name

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     describe
        (ALPHA) Provide information about a resource.

     list
        (ALPHA) List resources in a deployment.

EXAMPLES
    To view all details about a resource, run:

        $ gcloud alpha deployment-manager resources describe my-resource \
            --deployment my-deployment

    To see the list of all resources in a deployment, run:

        $ gcloud alpha deployment-manager resources list \
            --deployment my-deployment

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you will have to apply for early access and have your projects
    registered on the API whitelist to use it. To do so, contact Support at
    https://cloud.google.com/support/. These variants are also available:

        $ gcloud deployment-manager resources
        $ gcloud beta deployment-manager resources

