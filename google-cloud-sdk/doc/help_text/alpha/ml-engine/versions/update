NAME
    gcloud alpha ml-engine versions update - update a Cloud ML Engine version

SYNOPSIS
    gcloud alpha ml-engine versions update (VERSION : --model=MODEL)
        [--description=DESCRIPTION] [--update-labels=[KEY=VALUE,...]]
        [--add-package-uris=[PACKAGE-URIS,...] | --clear-package-uris
          | --remove-package-uris=[PACKAGE-URIS,...]
          | --set-package-uris=[PACKAGE-URIS,...] --clear-model-class
          | --model-class=MODEL_CLASS]
        [--clear-labels | --remove-labels=[KEY,...]] [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Update a Cloud ML Engine version.

POSITIONAL ARGUMENTS
     Version resource - The Cloud ML Engine model to update. The arguments in
     this group can be used to specify the attributes of this resource. (NOTE)
     Some attributes are not given arguments in this group but can be set in
     other ways. To set the [project] attribute: provide the flag [version] on
     the command line with a fully specified name; provide the flag [--project]
     on the command line; set the property [core/project]. This must be
     specified.

       VERSION
          The ID of the version or a fully qualified identifier for the
          version. This positional must be specified if any of the other
          arguments in this group are specified.

       --model=MODEL
          The model for the version.

FLAGS
     --description=DESCRIPTION
        The description of the version.

     --update-labels=[KEY=VALUE,...]
        A list of label KEY=VALUE pairs to update. If a label exists its value
        is modified, otherwise a new label is created.

        Keys must start with a lowercase character and contain only hyphens
        (-), underscores (_), lowercase characters, and numbers. Values must
        contain only hyphens (-), underscores (_), lowercase characters, and
        numbers.

     Configure user code in prediction. Cloud ML Engine allows a model to have
     user-provided prediction code; these options configure that code.

       These flags modify the member user-supplied packages to use for
       prediction of this version. The values should be given as a
       comma-separated list of Google Cloud Storage URIs (gs://...). At most
       one of these may be specified:

         --add-package-uris=[PACKAGE-URIS,...]
            Append the given values to the current user-supplied packages to
            use for prediction.

         --clear-package-uris
            Empty the current user-supplied packages to use for prediction.

         --remove-package-uris=[PACKAGE-URIS,...]
            Remove the given values from the current user-supplied packages to
            use for prediction.

         --set-package-uris=[PACKAGE-URIS,...]
            Completely replace the current user-supplied packages to use for
            prediction with the given values.

       The fully-qualified name of the custom Model class in the package
       provided for custom prediction. For example, my_package.SequenceModel.
       At most one of these may be specified:

         --clear-model-class
            Clear the custom Model class from the version.

         --model-class=MODEL_CLASS
            Set the custom Model class for the version.

     At most one of these may be specified:

       --clear-labels
          Removes all labels. If --update-labels is also specified then
          --clear-labels is applied first.

          For example, to remove all labels:

              $ gcloud alpha ml-engine versions update --clear-labels

          To set the labels to exactly "foo" and "baz":

              $ gcloud alpha ml-engine versions update --clear-labels \
                --update-labels foo=bar,baz=qux

       --remove-labels=[KEY,...]
          A list of label keys to remove. If a label does not exist it is
          silently ignored.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you will have to apply for early access and have your projects
    registered on the API whitelist to use it. To do so, contact Support at
    https://cloud.google.com/support/. These variants are also available:

        $ gcloud ml-engine versions update
        $ gcloud beta ml-engine versions update

