NAME
    gcloud alpha domains registrations configure - configure Cloud Domains
        registrations' management, DNS or contact settings

SYNOPSIS
    gcloud alpha domains registrations configure COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Commands for configuring Cloud Domains registrations' management,
    DNS or contact settings.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     contacts
        (ALPHA) Configure contact settings of a Cloud Domains registration.

     dns
        (ALPHA) Configure DNS settings of a Cloud Domains registration.

     management
        (ALPHA) Configure management settings of a Cloud Domains registration.

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you may be trying to access an API with an invitation-only early
    access allowlist.

