NAME
    gcloud alpha firebase test ios models - explore iOS models available in the
        Test Environment catalog

SYNOPSIS
    gcloud alpha firebase test ios models COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Explore iOS models available in the Test Environment catalog.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     describe
        (ALPHA) Describe an iOS model.

     list
        (ALPHA) List all iOS models available for testing.

EXAMPLES
    To list all iOS models available for running tests, along with their model
    names and supported iOS versions, run:

        $ gcloud alpha firebase test ios models list

    To view information about a single iOS model, run:

        $ gcloud alpha firebase test ios models describe MODEL_ID

NOTES
    This command is currently in ALPHA and may change without notice. Usually,
    users of ALPHA commands and flags need to apply for access, agree to
    applicable terms, and have their projects whitelisted. Contact Google or
    sign up on a product's page for ALPHA access. Product pages can be found at
    https://cloud.google.com/products/. This variant is also available:

        $ gcloud beta firebase test ios models

