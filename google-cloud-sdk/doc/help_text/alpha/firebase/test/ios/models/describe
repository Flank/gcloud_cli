NAME
    gcloud alpha firebase test ios models describe - describe an iOS model

SYNOPSIS
    gcloud alpha firebase test ios models describe MODEL_ID
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Describe an iOS model.

POSITIONAL ARGUMENTS
     MODEL_ID
        ID of the model to describe, found using $ gcloud alpha firebase test
        ios models list.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity.

    Run $ gcloud help for details.

EXAMPLES
    To describe an iOS model, run:

        gcloud alpha firebase test ios models describe iphone7

    To describe an iOS model in JSON format, run:

        gcloud alpha firebase test ios models describe iphone7 --format=json

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you may be trying to access an API with an invitation-only early
    access allowlist. These variants are also available:

        $ gcloud firebase test ios models describe
        $ gcloud beta firebase test ios models describe

