NAME
    gcloud alpha healthcare fhir-stores - manage Cloud Healthcare API FHIR
        stores

SYNOPSIS
    gcloud alpha healthcare fhir-stores COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Cloud Healthcare API FHIR stores.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity. Run $ gcloud help for
    details.

COMMANDS
    COMMAND is one of the following:

     add-iam-policy-binding
        (ALPHA) Add an IAM policy binding to a Cloud Healthcare API FHIR store.

     create
        (ALPHA) Create a Cloud Healthcare API FHIR store.

     delete
        (ALPHA) Delete a Cloud Healthcare API FHIR store.

     describe
        (ALPHA) Describe a Cloud Healthcare API FHIR store.

     export
        (ALPHA) Export Cloud Healthcare API FHIR resources.

     get-iam-policy
        (ALPHA) Retrieve the IAM policy for a Cloud Healthcare API FHIR store.

     import
        (ALPHA) Import FHIR resources into a Cloud Healthcare API FHIR store.

     list
        (ALPHA) List Cloud Healthcare API FHIR stores.

     remove-iam-policy-binding
        (ALPHA) Remove an IAM policy binding from a Cloud Healthcare API FHIR
        store.

     set-iam-policy
        (ALPHA) Set the IAM policy for a Cloud Healthcare API FHIR store.

     update
        (ALPHA) Update a Cloud Healthcare API FHIR store.

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you will have to apply for early access and have your projects
    registered on the API whitelist to use it. To do so, contact Support at
    https://cloud.google.com/support/.

