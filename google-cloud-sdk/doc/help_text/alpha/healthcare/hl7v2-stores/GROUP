NAME
    gcloud alpha healthcare hl7v2-stores - manage Cloud Healthcare API HL7v2
        stores

SYNOPSIS
    gcloud alpha healthcare hl7v2-stores COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Cloud Healthcare API HL7v2 stores.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     add-iam-policy-binding
        (ALPHA) Add an IAM policy binding to a Cloud Healthcare API HL7v2
        store.

     create
        (ALPHA) Create a Cloud Healthcare API HL7v2 store.

     delete
        (ALPHA) Delete a Cloud Healthcare API HL7v2 store.

     describe
        (ALPHA) Describe a Cloud Healthcare API HL7v2 store.

     get-iam-policy
        (ALPHA) Retrieve the IAM policy for a Cloud Healthcare API HL7v2 store.

     list
        (ALPHA) List Cloud Healthcare API HL7v2 stores.

     remove-iam-policy-binding
        (ALPHA) Remove an IAM policy binding from a Cloud Healthcare API HL7v2
        store.

     set-iam-policy
        (ALPHA) Set the IAM policy for a Cloud Healthcare API HL7v2 store.

     update
        (ALPHA) Update a Cloud Healthcare API HL7v2 store.

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you may be trying to access an API with an invitation-only early
    access whitelist. This variant is also available:

        $ gcloud beta healthcare hl7v2-stores

