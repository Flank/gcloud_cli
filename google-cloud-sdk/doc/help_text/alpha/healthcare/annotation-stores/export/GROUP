NAME
    gcloud alpha healthcare annotation-stores export - manage Cloud Healthcare
        API Annotation store exports

SYNOPSIS
    gcloud alpha healthcare annotation-stores export COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Cloud Healthcare API Annotation store exports.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     bq
        (ALPHA) Export Cloud Healthcare API Annotation records to BigQuery.

     gcs
        (ALPHA) Export Cloud Healthcare API Annotation records to Google Cloud
        Storage.

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you may be trying to access an API with an invitation-only early
    access allowlist. This variant is also available:

        $ gcloud beta healthcare annotation-stores export

