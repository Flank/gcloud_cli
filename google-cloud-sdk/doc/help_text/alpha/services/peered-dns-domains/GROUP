NAME
    gcloud alpha services peered-dns-domains - peered DNS domains for various
        private service connections

SYNOPSIS
    gcloud alpha services peered-dns-domains COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Peered DNS domains for various private service connections.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create a peered DNS domain for a private service connection.

     delete
        (ALPHA) Delete a peered DNS domain for a private service connection.

     list
        (ALPHA) List the peered DNS domains for a private service connection.

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you may be trying to access an API with an invitation-only early
    access allowlist. These variants are also available:

        $ gcloud services peered-dns-domains
        $ gcloud beta services peered-dns-domains

