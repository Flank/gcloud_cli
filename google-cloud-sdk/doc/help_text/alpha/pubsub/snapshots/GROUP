NAME
    gcloud alpha pubsub snapshots - these features are part of an invite-only
        release of the Cloud Pub/Sub API

SYNOPSIS
    gcloud alpha pubsub snapshots COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Cloud Pub/Sub snapshots. These features are part of an
    invitation-only release of the underlying Cloud Pub/Sub API. These commands
    will generate errors unless you have access to this API. This restriction
    should be relaxed in the near future. Please contact
    cloud-pubsub@google.com with any questions in the meantime.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) This feature is part of an invite-only release of the Cloud
        Pub/Sub API.

     delete
        (ALPHA) This feature is part of an invite-only release of the Cloud
        Pub/Sub API.

     describe
        (ALPHA) This feature is part of an invite-only release of the Cloud
        Pub/Sub API.

     list
        (ALPHA) This feature is part of an invite-only release of the Cloud
        Pub/Sub API.

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you will have to apply for early access and have your projects
    registered on the API whitelist to use it. To do so, contact Support at
    https://cloud.google.com/support/.

