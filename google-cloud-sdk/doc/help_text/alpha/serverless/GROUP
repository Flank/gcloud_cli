NAME
    gcloud alpha serverless - manage your Serverless applications

SYNOPSIS
    gcloud alpha serverless COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) The gcloud serverless command group lets you deploy apps, functions
    and containers to Google Serverless Engine.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     build
        (ALPHA) Remote build a Serverless image artifact from source code.

EXAMPLES
    To deploy your application, use the gcloud serverless deploy command:

        $ gcloud serverless deploy <service-name> --image <image_name>

    For more information, run:        $ gcloud serverless deploy --help

NOTES
    This command is an internal implementation detail and may change or
    disappear without notice. This command is currently in ALPHA and may change
    without notice. If this command fails with API permission errors despite
    specifying the right project, you will have to apply for early access and
    have your projects registered on the API whitelist to use it. To do so,
    contact Support at https://cloud.google.com/support/.

