NAME
    gcloud alpha data-catalog - manage Cloud Data Catalog resources

SYNOPSIS
    gcloud alpha data-catalog GROUP | COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Cloud Data Catalog resources.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity. Run $ gcloud help for
    details.

GROUPS
    GROUP is one of the following:

     crawler-runs
        (ALPHA) Manage crawler runs in Cloud Data Catalog.

     crawlers
        (ALPHA) Manage crawler configurations in Cloud Data Catalog.

     entries
        (ALPHA) Manage entries in Cloud Data Catalog.

     entry-groups
        (ALPHA) Manage entry groups in Cloud Data Catalog.

     tag-templates
        (ALPHA) Manage tag templates in Cloud Data Catalog.

     tags
        (ALPHA) Manage tags in Cloud Data Catalog.

COMMANDS
    COMMAND is one of the following:

     search
        (ALPHA) Search Cloud Data Catalog for resources that match a query.

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you may be trying to access an API with an invitation-only early
    access whitelist. This variant is also available:

        $ gcloud beta data-catalog

