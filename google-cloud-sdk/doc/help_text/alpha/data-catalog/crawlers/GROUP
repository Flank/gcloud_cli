NAME
    gcloud alpha data-catalog crawlers - manage crawler configurations in Cloud
        Data Catalog

SYNOPSIS
    gcloud alpha data-catalog crawlers COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage crawler configurations in Cloud Data Catalog.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create a new Cloud Data Catalog crawler.

     delete
        (ALPHA) Delete a Cloud Data Catalog crawler.

     describe
        (ALPHA) Describe a Cloud Data Catalog crawler.

     list
        (ALPHA) List Cloud Data Catalog crawlers.

     update
        (ALPHA) Update a Cloud Data Catalog crawler.

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you may be trying to access an API with an invitation-only early
    access allowlist.

