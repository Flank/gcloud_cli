NAME
    gcloud alpha data-catalog entry-groups - manage entry groups in Cloud Data
        Catalog

SYNOPSIS
    gcloud alpha data-catalog entry-groups COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage entry groups in Cloud Data Catalog.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     add-iam-policy-binding
        (ALPHA) Add an IAM policy binding to a Cloud Data Catalog entry group.

     create
        (ALPHA) Create a Cloud Data Catalog entry group.

     delete
        (ALPHA) Delete a Cloud Data Catalog entry group.

     describe
        (ALPHA) Describe a Cloud Data Catalog entry group.

     get-iam-policy
        (ALPHA) Get the IAM policy for a Cloud Data Catalog entry group.

     list
        (ALPHA) List all entry groups in a Cloud Data Catalog location.

     remove-iam-policy-binding
        (ALPHA) Remove an IAM policy binding from a Cloud Data Catalog entry
        group.

     set-iam-policy
        (ALPHA) Set the IAM policy for a Cloud Data Catalog entry group.

     update
        (ALPHA) Update a Cloud Data Catalog entry group.

NOTES
    This command is currently in ALPHA and may change without notice. If this
    command fails with API permission errors despite specifying the right
    project, you may be trying to access an API with an invitation-only early
    access whitelist. These variants are also available:

        $ gcloud data-catalog entry-groups
        $ gcloud beta data-catalog entry-groups

