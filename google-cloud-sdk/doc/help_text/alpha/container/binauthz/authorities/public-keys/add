NAME
    gcloud alpha container binauthz authorities public-keys add - add a public
        key to an Attestation Authority

SYNOPSIS
    gcloud alpha container binauthz authorities public-keys add
        --authority=AUTHORITY --public-key-file=PUBLIC_KEY_FILE
        [--comment=COMMENT] [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Add a public key to an Attestation Authority.

REQUIRED FLAGS
     Authority resource - The authority to which the public key should be
     added. This represents a Cloud resource. (NOTE) Some attributes are not
     given arguments in this group but can be set in other ways. To set the
     [project] attribute: provide the flag [--authority] on the command line
     with a fully specified name; provide the flag [--project] on the command
     line; set the property [core/project]. This must be specified.

       --authority=AUTHORITY
          The ID of the authority or a fully qualified identifier for the
          authority.

     --public-key-file=PUBLIC_KEY_FILE
        The path to the file containing the ASCII-armored PGP public key to
        add.

OPTIONAL FLAGS
     --comment=COMMENT
        The comment describing the public key.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

NOTES
    This command is an internal implementation detail and may change or
    disappear without notice. This command is currently in ALPHA and may change
    without notice. Usually, users of ALPHA commands and flags need to apply
    for access, agree to applicable terms, and have their projects whitelisted.
    Contact Google or sign up on a product's page for ALPHA access. Product
    pages can be found at https://cloud.google.com/products/.

