NAME
    gcloud beta bigtable clusters update - update a Bigtable cluster's friendly
        name and serving nodes

SYNOPSIS
    gcloud beta bigtable clusters update CLUSTER --instance=INSTANCE
        --num-nodes=NUM_NODES [--async] [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Update a Bigtable cluster's friendly name and serving nodes.

POSITIONAL ARGUMENTS
     CLUSTER
        ID of the cluster.

REQUIRED FLAGS
     --instance=INSTANCE
        ID of the instance.

     --num-nodes=NUM_NODES
        Number of nodes to serve.

OPTIONAL FLAGS
     --async
        Return immediately, without waiting for operation to complete.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

NOTES
    This command is currently in BETA and may change without notice.

