NAME
    gcloud beta api-gateway gateways describe - show details about a specific
        gateway

SYNOPSIS
    gcloud beta api-gateway gateways describe (GATEWAY : --location=LOCATION)
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Show details about a specific gateway.

POSITIONAL ARGUMENTS
     Gateway resource - Name for gateway which will be created. The arguments
     in this group can be used to specify the attributes of this resource.
     (NOTE) Some attributes are not given arguments in this group but can be
     set in other ways. To set the [project] attribute: provide the argument
     [gateway] on the command line with a fully specified name; provide the
     argument [--project] on the command line; set the property [core/project].
     This must be specified.

       GATEWAY
          ID of the gateway or fully qualified identifier for the gateway. This
          positional must be specified if any of the other arguments in this
          group are specified.

       --location=LOCATION
          Cloud location for gateway.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity.

    Run $ gcloud help for details.

EXAMPLES
    To show details about a Gateway in us-central1, run:

        $ gcloud beta api-gateway gateways describe my-gateway \
            --location=us-central1

NOTES
    This command is currently in BETA and may change without notice. This
    variant is also available:

        $ gcloud alpha api-gateway gateways describe

