NAME
    gcloud beta recommender recommendations - manage Cloud resource
        recommendations

SYNOPSIS
    gcloud beta recommender recommendations COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Manage Cloud resource recommendations.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity. Run $ gcloud help for
    details.

COMMANDS
    COMMAND is one of the following:

     describe
        (BETA) Describe a recommendation for a Cloud project.

     list
        (BETA) Lists recommendations for a Cloud project.

     mark-claimed
        (BETA) Mark the Recommendation State as CLAIMED. Can only be performed
        on recommendations in CLAIMED or ACTIVE state.

     mark-failed
        (BETA) Mark a recommendation's state as FAILED. mark-failed can be
        applied to recommendations in ACTIVE, CLAIMED, SUCCEEDED, or FAILED
        state.

     mark-succeeded
        (BETA) Mark a recommendation's state as SUCCEEDED. mark-succeeded can
        be applied to recommendations in ACTIVE, CLAIMED, SUCCEEDED, or FAILED
        state.

NOTES
    This command is currently in BETA and may change without notice. This
    variant is also available:

        $ gcloud alpha recommender recommendations

