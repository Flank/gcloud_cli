NAME
    gcloud beta compute machine-types - read Google Compute Engine virtual
        machine types

SYNOPSIS
    gcloud beta compute machine-types COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Read Google Compute Engine virtual machine types.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     describe
        (BETA) Describe a Google Compute Engine machine type.

     list
        (BETA) List Google Compute Engine machine types.

NOTES
    This command is currently in BETA and may change without notice. These
    variants are also available:

        $ gcloud compute machine-types
        $ gcloud alpha compute machine-types

