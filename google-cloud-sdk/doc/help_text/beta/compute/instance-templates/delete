NAME
    gcloud beta compute instance-templates delete - delete Google Compute
        Engine virtual machine instance templates

SYNOPSIS
    gcloud beta compute instance-templates delete NAME [NAME ...]
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) gcloud beta compute instance-templates delete deletes one or more
    Google Compute Engine virtual machine instance templates.

POSITIONAL ARGUMENTS
     NAME [NAME ...]
        Names of the instance templates to delete.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity.

    Run $ gcloud help for details.

EXAMPLES
    To delete the instance template named 'INSTANCE-TEMPLATE', run:

        $ gcloud beta compute instance-templates delete INSTANCE-TEMPLATE

NOTES
    This command is currently in BETA and may change without notice. These
    variants are also available:

        $ gcloud compute instance-templates delete
        $ gcloud alpha compute instance-templates delete

