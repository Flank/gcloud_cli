NAME
    gcloud beta compute groups add-members - add a user to a Google Compute
        Engine group

SYNOPSIS
    gcloud beta compute groups add-members NAME [NAME ...]
        --members=USERNAME,[USERNAME,...] [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) gcloud beta compute groups add-members adds a users to a Google
    Compute Engine group.

POSITIONAL ARGUMENTS
     NAME [NAME ...]
        The names of the groups to add members to.

REQUIRED FLAGS
     --members=USERNAME,[USERNAME,...]
        The names or fully-qualified URLs of the users to add.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

EXAMPLES
    To add a user to a group, run:

        $ gcloud beta compute groups add-members example-group \
            --members example-user

    To add multiple users to multiple groups, run:

        $ gcloud beta compute groups add-members example-group-1 \
            example-group-2 --members example-user-1,example-user-2

NOTES
    This command is currently in BETA and may change without notice. This
    variant is also available:

        $ gcloud alpha compute groups add-members

