NAME
    gcloud beta compute routers nats - list, create, describe and delete Google
        Compute Engine NATs

SYNOPSIS
    gcloud beta compute routers nats COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) List, create, describe and delete Google Compute Engine NATs.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flags-file, --flatten, --format, --help, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity. Run $ gcloud help for
    details.

COMMANDS
    COMMAND is one of the following:

     create
        (BETA) Add a NAT to a Google Compute Engine router.

     delete
        (BETA) Remove a NAT from a Google Compute Engine router.

     describe
        (BETA) Describe a NAT in a Google Compute Engine router.

     list
        (BETA) Lists the NATs on a Google Compute Engine router.

     update
        (BETA) Update a NAT on a Google Compute Engine router.

NOTES
    This command is currently in BETA and may change without notice. These
    variants are also available:

        $ gcloud compute routers nats
        $ gcloud alpha compute routers nats

