NAME
    gcloud beta compute tpus locations - list or Describe Available Cloud TPU
        Locations

SYNOPSIS
    gcloud beta compute tpus locations COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) List or Describe Available Cloud TPU Locations.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flags-file, --flatten, --format, --help, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity. Run $ gcloud help for
    details.

COMMANDS
    COMMAND is one of the following:

     describe
        (BETA) Describe a Cloud TPU Location.

     list
        (BETA) List available locations for Cloud TPUs.

NOTES
    This command is currently in BETA and may change without notice. These
    variants are also available:

        $ gcloud compute tpus locations
        $ gcloud alpha compute tpus locations

