NAME
    gcloud beta compute networks describe - describe a Google Compute Engine
        network

SYNOPSIS
    gcloud beta compute networks describe NAME [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) gcloud beta compute networks describe displays all data associated
    with Google Compute Engine network in a project.

POSITIONAL ARGUMENTS
     NAME
        Name of the network to describe.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity.

    Run $ gcloud help for details.

EXAMPLES
    To describe a network with the name 'network-name', run:

        $ gcloud beta compute networks describe network-name

NOTES
    This command is currently in BETA and may change without notice. These
    variants are also available:

        $ gcloud compute networks describe
        $ gcloud alpha compute networks describe

