NAME
    gcloud beta compute xpn get-host-project - get the shared VPC host project
        that the given project is associated with

SYNOPSIS
    gcloud beta compute xpn get-host-project PROJECT_ID [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Get the shared VPC host project that the given project is associated
    with.

POSITIONAL ARGUMENTS
     PROJECT_ID
        ID for the project to get the host project for

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

EXAMPLES
    If service-project1 and service-project2 are associated service projects of
    the shared VPC host project host-project,

        $ gcloud beta compute xpn get-host-project service-project1

    will show the host-project project.

NOTES
    This command is currently in BETA and may change without notice. These
    variants are also available:

        $ gcloud compute xpn get-host-project
        $ gcloud alpha compute xpn get-host-project

