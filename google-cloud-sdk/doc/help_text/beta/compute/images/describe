NAME
    gcloud beta compute images describe - describe a Google Compute Engine
        image

SYNOPSIS
    gcloud beta compute images describe IMAGE_NAME [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) gcloud beta compute images describe displays all data associated
    with a Google Compute Engine image in a project.

POSITIONAL ARGUMENTS
     IMAGE_NAME
        Name of the disk image to describe.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

NOTES
    This command is currently in BETA and may change without notice. These
    variants are also available:

        $ gcloud compute images describe
        $ gcloud alpha compute images describe

