NAME
    gcloud beta compute url-maps delete - delete URL maps

SYNOPSIS
    gcloud beta compute url-maps delete URL_MAP_NAME [URL_MAP_NAME ...]
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) gcloud beta compute url-maps delete deletes one or more URL maps.

POSITIONAL ARGUMENTS
     URL_MAP_NAME [URL_MAP_NAME ...]
        Names of the URL maps to delete.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

NOTES
    This command is currently in BETA and may change without notice. These
    variants are also available:

        $ gcloud compute url-maps delete
        $ gcloud alpha compute url-maps delete

