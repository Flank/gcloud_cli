NAME
    gcloud beta ml-engine jobs submit - cloud ML Engine Jobs submit commands

SYNOPSIS
    gcloud beta ml-engine jobs submit COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Cloud ML Engine Jobs submit commands.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     prediction
        (BETA) Start a Cloud ML Engine batch prediction job.

     training
        (BETA) Submits a Cloud Machine Learning training job.

NOTES
    This command is currently in BETA and may change without notice. These
    variants are also available:

        $ gcloud ml-engine jobs submit
        $ gcloud alpha ml-engine jobs submit

