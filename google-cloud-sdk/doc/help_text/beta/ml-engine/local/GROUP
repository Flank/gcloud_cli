NAME
    gcloud beta ml-engine local - AI Platform Local commands

SYNOPSIS
    gcloud beta ml-engine local COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) AI Platform Local commands.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity. Run $ gcloud help for
    details.

COMMANDS
    COMMAND is one of the following:

     predict
        (BETA) Run prediction locally.

     train
        (BETA) Run an AI Platform training job locally.

NOTES
    This command is currently in BETA and may change without notice. These
    variants are also available:

        $ gcloud ml-engine local
        $ gcloud alpha ml-engine local

