NAME
    gcloud beta ml-engine operations - manage Cloud ML Engine operations

SYNOPSIS
    gcloud beta ml-engine operations COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Manage Cloud ML Engine operations.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     cancel
        (BETA) Cancel a Cloud ML Engine operation.

     delete
        (BETA) Delete a Cloud ML Engine operation.

     describe
        (BETA) Describe a Cloud ML Engine operation.

     list
        (BETA) List existing Cloud ML Engine jobs.

     wait
        (BETA) Wait for a Cloud ML Engine operation to complete.

NOTES
    This command is currently in BETA and may change without notice. These
    variants are also available:

        $ gcloud ml-engine operations
        $ gcloud alpha ml-engine operations

