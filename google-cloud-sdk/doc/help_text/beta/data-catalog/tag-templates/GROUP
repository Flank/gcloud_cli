NAME
    gcloud beta data-catalog tag-templates - manage tag templates in Cloud Data
        Catalog

SYNOPSIS
    gcloud beta data-catalog tag-templates GROUP | COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Manage tag templates in Cloud Data Catalog.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity.

    Run $ gcloud help for details.

GROUPS
    GROUP is one of the following:

     fields
        (BETA) Manage tag template fields in Cloud Data Catalog.

COMMANDS
    COMMAND is one of the following:

     add-iam-policy-binding
        (BETA) Add IAM policy binding to a Cloud Data Catalog tag template.

     create
        (BETA) Create a Cloud Data Catalog tag template.

     delete
        (BETA) Delete a Cloud Data Catalog tag template.

     describe
        (BETA) Describe a Cloud Data Catalog tag template.

     get-iam-policy
        (BETA) Get the IAM policy for a Cloud Data Catalog tag template.

     remove-iam-policy-binding
        (BETA) Remove IAM policy binding from a Cloud Data Catalog tag
        template.

     set-iam-policy
        (BETA) Set the IAM policy for a Cloud Data Catalog tag template.

     update
        (BETA) Update a Cloud Data Catalog tag template.

NOTES
    This command is currently in BETA and may change without notice. These
    variants are also available:

        $ gcloud data-catalog tag-templates
        $ gcloud alpha data-catalog tag-templates

