NAME
    gcloud beta healthcare annotation-stores evaluate - manage Cloud Healthcare
        API Annotation store evaluates

SYNOPSIS
    gcloud beta healthcare annotation-stores evaluate COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Manage Cloud Healthcare API Annotation store evaluates.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     bq
        (BETA) Evaluate a Cloud Healthcare API Annotation store and report to
        BigQuery.

NOTES
    This command is currently in BETA and may change without notice. This
    variant is also available:

        $ gcloud alpha healthcare annotation-stores evaluate

