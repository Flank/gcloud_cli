NAME
    gcloud beta functions regions - list regions available to Google Cloud
        Functions

SYNOPSIS
    gcloud beta functions regions COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) List regions available to Google Cloud Functions.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     list
        (BETA) List regions available to Google Cloud Functions.

NOTES
    This command is currently in BETA and may change without notice. This
    variant is also available:

        $ gcloud alpha functions regions

