NAME
    gcloud beta auth print-access-token - print an access token for the active
        account

SYNOPSIS
    gcloud beta auth print-access-token [ACCOUNT] [--force-auth-refresh]
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Print an access token for the active account.

POSITIONAL ARGUMENTS
     [ACCOUNT]
        The account to get the access token for. Leave empty for the active
        account.

FLAGS
     --force-auth-refresh
        Force a refresh of the credentials even if they have not expired yet.
        By default, credentials will only be refreshed when necessary.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity. Run $ gcloud help for
    details.

EXAMPLES
    To print access tokens:

        $ gcloud beta auth print-access-token

NOTES
    This command is currently in BETA and may change without notice. These
    variants are also available:

        $ gcloud auth print-access-token
        $ gcloud alpha auth print-access-token

