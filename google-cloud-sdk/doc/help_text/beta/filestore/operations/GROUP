NAME
    gcloud beta filestore operations - read and manipulate Cloud Filestore
        operations

SYNOPSIS
    gcloud beta filestore operations COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Read and manipulate Cloud Filestore operations.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     describe
        (BETA) Describe a Cloud Filestore operation.

     list
        (BETA) List all Cloud Filestore operations.

NOTES
    This command is currently in BETA and may change without notice. These
    variants are also available:

        $ gcloud filestore operations
        $ gcloud alpha filestore operations

