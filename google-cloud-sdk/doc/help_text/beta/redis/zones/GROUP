NAME
    gcloud beta redis zones - manage Cloud Memorystore Redis zones

SYNOPSIS
    gcloud beta redis zones COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Manage Cloud Memorystore Redis zones.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --billing-project,
    --configuration, --flags-file, --flatten, --format, --help,
    --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity. Run $ gcloud help for
    details.

COMMANDS
    COMMAND is one of the following:

     list
        (BETA) List Memorystore Redis zones.

NOTES
    This command is currently in BETA and may change without notice. These
    variants are also available:

        $ gcloud redis zones
        $ gcloud alpha redis zones

