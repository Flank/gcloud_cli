NAME
    gcloud beta sql import - provides commands to import Cloud SQL instances

SYNOPSIS
    gcloud beta sql import COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Provides commands to import Cloud SQL instances.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     csv
        (BETA) Imports data into a Cloud SQL instance from a CSV file.

     sql
        (BETA) Imports data into a Cloud SQL instance from a SQL dump file.

NOTES
    This command is currently in BETA and may change without notice. This
    variant is also available:

        $ gcloud sql import

