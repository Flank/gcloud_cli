NAME
    gcloud iam service-accounts keys delete - delete a user-managed key from a
        service account

SYNOPSIS
    gcloud iam service-accounts keys delete KEY-ID --iam-account=IAM_ACCOUNT
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    Delete a user-managed key from a service account.

POSITIONAL ARGUMENTS
     KEY-ID
        The key to delete.

REQUIRED FLAGS
     --iam-account=IAM_ACCOUNT
        The service account whose key to delete.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flatten, --format, --help, --log-http, --project, --quiet, --trace-token,
    --user-output-enabled, --verbosity. Run $ gcloud help for details.

NOTES
    These variants are also available:

        $ gcloud alpha iam service-accounts keys delete
        $ gcloud beta iam service-accounts keys delete

