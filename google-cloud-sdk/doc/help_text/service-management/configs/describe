NAME
    gcloud service-management configs describe - describes the configuration
        for a given version of a service

SYNOPSIS
    gcloud service-management configs describe CONFIG_ID [--service=SERVICE]
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (REMOVED) Describes the configuration for a given version of a service.

    The service-management configs describe command has been replaced by
    endpoints configs describe.

    This command prints out the configuration for the given version of a given
    service. You specify the name of the service and the ID of the
    configuration, and the command will print out the specified config.

POSITIONAL ARGUMENTS
     CONFIG_ID
        The configuration ID to retrieve.

FLAGS
     --service=SERVICE
        The name of the service from which to retrieve the configuration..

GCLOUD WIDE FLAGS
    These flags are available to all commands: --account, --configuration,
    --flags-file, --flatten, --format, --help, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity. Run $ gcloud help for
    details.

EXAMPLES
    To print the configuration with ID 2017-01-01R0 for the service called
    my-service, run:

        $ gcloud service-management configs describe --service my-service \
            2017-01-01R0

NOTES
    This command is an internal implementation detail and may change or
    disappear without notice.

